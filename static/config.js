/* ============================================================
   MailFlow — Frontend Backend Configuration
   ============================================================
   When this dashboard is served by the Python backend itself
   (running locally or on Render/Railway), leave this EMPTY.

   When you deploy ONLY this static folder to Vercel, set the URL
   of your separately-hosted Python backend below, e.g.:

       window.MAILFLOW_API_BASE = "https://mailflow-api.onrender.com";
   ============================================================ */
window.MAILFLOW_API_BASE = "";
